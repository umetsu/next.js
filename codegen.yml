overwrite: true
schema: "schema.graphql"
documents: "src/**/*.tsx"
generates:
  src/graphql/generated/types.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "fragment-matcher"